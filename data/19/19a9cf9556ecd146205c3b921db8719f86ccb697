<p>Late to the party, but investing in going paperless. Got a scanner with OCR, which generates searchable pdfs. If I could only grep through them…</p>
<pre><code class="language-{.bash">brew install pdfgrep
</code></pre>
<p>Balance restored.</p>
