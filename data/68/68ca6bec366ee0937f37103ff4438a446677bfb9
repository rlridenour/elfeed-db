<p>
It has been some time since I came along the idea of the <a href="https://whatacold.io/blog/2023-06-12-emacs-join-lines/">w/join-lines</a> command to join lines. After that, sometimes I found that it would be even better to join every a few lines.</p>
<p>
Let&#39;s see the example below, suppose we&#39;ve copies some data from somewhere, and now we want to yank it into an Emacs buffer and slightly modify it to be like an matrix.</p>
<p>
That is, make it from:</p>