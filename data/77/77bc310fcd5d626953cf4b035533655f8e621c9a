 Some of my Clojure deployments are using our old deployment method, which uses immutant, but it&rsquo;s deprecated and fails due to an old dependency requiring java 8-. So switch to java 18 like (assuming it&rsquo;s installed) like this:
sudo update-alternatives --config java Resources 