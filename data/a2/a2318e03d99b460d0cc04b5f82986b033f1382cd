Intro I use Firefox Mobile, which tries to install the PWA (Progressive Web App) of sites whenever it finds a manifest file; otherwise it allows installation to just add a Firefox link to the site instead, which is what I want.
This can be done with the Ublock plugin1. I&rsquo;m struggling to find the right syntax for this; I believe I want a static filter that applies to any site. An example of the line that brings the manifest from Google Photos is this: