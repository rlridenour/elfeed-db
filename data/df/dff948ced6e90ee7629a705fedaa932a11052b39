<p>Recently needed to rotate images based on EXIF metadata. ImageMagick to the rescue:</p>
<pre><code class="language-{.bash">$ for i in *.png; do convert -auto-orient &quot;$i&quot; &quot;$i&quot;; done
</code></pre>
